{% extends 'base.html.twig' %}

{% block title %}{{ prestation.titre }} - Créa'Tech{% endblock %}

{% block description %}
    Découvrez notre offre {{ prestation.titre }}. {{ prestation.descriptionCourte }}
{% endblock %}

{% block body %}
    {# --- HERO HEADER : Titre et Ambiance --- #}
    <div class="relative bg-base-200 py-20 overflow-hidden">
        {# Fond abstrait #}
        <div class="absolute inset-0 opacity-10 pointer-events-none" 
            style="background-image: radial-gradient(circle at 70% 30%, #6419e6 0%, transparent 50%), radial-gradient(circle at 10% 80%, #d926a9 0%, transparent 40%);">
        </div>

        <div class="container mx-auto px-4 relative z-10 text-center md:text-left">
        <a href="{{ path('app_prestation_index') }}" class="btn btn-ghost btn-sm mb-6 opacity-60 hover:opacity-100">
                ← Retour aux services
            </a>
            <h1 class="text-4xl md:text-6xl font-extrabold mb-4 leading-tight">
                {{ prestation.titre }}
            </h1>
            <p class="text-xl md:text-2xl text-base-content/70 max-w-3xl leading-relaxed">
                {{ prestation.descriptionCourte }}
            </p>
            {% if prestation.prix %}
    <div class="text-2xl font-bold text-primary mt-4 mb-6">
        {# On formate le prix pour l'afficher proprement (ex: 500,00 €) #}
        Tarif à partir de : {{ prestation.prix }} € TTC
    </div>
{% endif %}
        </div>
    </div>

    {# --- CONTENU PRINCIPAL --- #}
    <div class="bg-base-100 min-h-screen">
        <div class="container mx-auto px-4 py-12">
            
            <div class="flex flex-col lg:flex-row gap-12">

                {# --- COLONNE GAUCHE : LE CONTENU (Large) --- #}
                <div class="w-full lg:w-2/3 order-2 lg:order-1">
                    
                    {# Image principale (Bannière) #}
                    <div class="rounded-2xl overflow-hidden shadow-2xl mb-12 ring-1 ring-base-content/10 bg-base-200">
                        {% if prestation.image %}
                            <img 
                                src="{{ asset('uploads/prestations/' ~ prestation.image) }}" 
                                alt="{{ prestation.titre }}" 
                                class="w-full h-auto object-cover max-h-[400px]" 
                            />
                        {% endif %}
                    </div>

                    {# ZONE DE TEXTE & HTML #}
                    {# On a retiré la classe 'prose' stricte pour laisser vos designs s'exprimer #}
                    <div class="space-y-6 text-base-content/90 leading-relaxed text-lg">
                        {% if prestation.contenu %}
                            {{ prestation.contenu|raw }}
                        {% else %}
                            <div class="alert">
                                <span>Contenu en cours de rédaction...</span>
                            </div>
                        {% endif %}
                    </div>

                </div>

                {# --- COLONNE DROITE : SIDEBAR (Sticky) --- #}
                <div class="w-full lg:w-1/3 order-1 lg:order-2">
                    <div class="sticky top-8 space-y-8">
                        
                        {# Carte d'action #}
                        <div class="card bg-base-200 shadow-xl border border-primary/20 overflow-hidden">
                            <div class="card-body">
                                <h3 class="text-xl font-bold mb-2">Ce service vous intéresse ?</h3>
                                <p class="text-sm opacity-70 mb-6">Discutons de votre besoin spécifique. Le premier échange est gratuit.</p>
                                
                                <a href="{{ path('app_contact', {'sujet': 'Info sur : ' ~ prestation.titre}) }}" class="btn btn-primary w-full shadow-lg hover:shadow-primary/50 transition-all">
                                    Demander un devis / Audit
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" /></svg>
                                </a>
                            </div>
                            {# Bandeau bas #}
                            <div class="bg-base-300 p-4 text-center text-xs opacity-60">
                                Réponse sous 24h ouvrées
                            </div>
                        </div>

                        {# Carte "Atouts" #}
                        <div class="hidden lg:block">
                            <h4 class="font-bold text-lg mb-4 flex items-center gap-2">
                                <span class="text-primary">●</span> Pourquoi Créa'Tech ?
                            </h4>
                            <ul class="space-y-4">
                                <li class="flex gap-3 items-start">
                                    <div class="mt-1 p-1 bg-green-500/20 rounded text-green-500 text-xs">✔</div>
                                    <div class="text-sm"><strong class="block text-base-content">Expertise Symfony</strong> Code robuste et durable.</div>
                                </li>
                                <li class="flex gap-3 items-start">
                                    <div class="mt-1 p-1 bg-blue-500/20 rounded text-blue-500 text-xs">✔</div>
                                    <div class="text-sm"><strong class="block text-base-content">Pas de sous-traitance</strong> Vous parlez directement au développeur.</div>
                                </li>
                                <li class="flex gap-3 items-start">
                                    <div class="mt-1 p-1 bg-purple-500/20 rounded text-purple-500 text-xs">✔</div>
                                    <div class="text-sm"><strong class="block text-base-content">Suivi personnalisé</strong> Vidéo de formation incluse à la livraison.</div>
                                </li>
                            </ul>
                        </div>

                    </div>
                </div>

            </div>
        </div>
    </div>
{% endblock %}