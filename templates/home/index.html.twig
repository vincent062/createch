{% extends 'base.html.twig' %}

{% block title %}Accueil - Crea'Tech{% endblock %}

{% block body %}

    {# --- SECTION HERO HIGH-TECH --- #}
    <div class="hero min-h-[85vh] relative overflow-hidden bg-base-300">
        
        {# Arrière-plan technique (Grille + Effet) #}
        <div class="absolute inset-0 z-0 opacity-20" 
             style="background-image: linear-gradient(#4b5563 1px, transparent 1px), linear-gradient(90deg, #4b5563 1px, transparent 1px); background-size: 40px 40px;">
        </div>
        <div class="absolute inset-0 bg-gradient-to-t from-base-300 via-transparent to-transparent z-0"></div>

        <div class="hero-content text-center relative z-10">
            <div class="max-w-3xl">
                
                {# Badge style "Console" #}
                <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-primary/30 bg-primary/10 text-primary text-xs font-mono mb-6 tracking-widest uppercase">
                    <span class="relative flex h-2 w-2">
                      <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
                      <span class="relative inline-flex rounded-full h-2 w-2 bg-primary"></span>
                    </span>
                    System Online v2.0
                </div>

                {# Titre avec effet dégradé #}
                <h1 class="text-5xl md:text-7xl font-extrabold leading-tight tracking-tight mb-6">
                    L'Innovation Digitale <br/>
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary via-secondary to-accent animate-pulse">
                        Code & Création
                    </span>
                </h1>

                <p class="py-6 text-lg md:text-xl opacity-80 max-w-2xl mx-auto leading-relaxed">
                    Transformez votre vision en réalité numérique. 
                    <span class="font-mono text-primary bg-base-100 px-1 rounded">&lt;Dev&gt;</span> 
                    Sites Web, 
                    <span class="font-mono text-secondary bg-base-100 px-1 rounded">{Apps}</span> 
                    Mobiles et Solutions Sur-Mesure.
                </p>

                {# Boutons Cyberpunk #}
                <div class="flex flex-col sm:flex-row gap-4 justify-center mt-4">
                    <a href="{{ path('app_contact') }}" class="btn btn-primary btn-lg shadow-[0_0_20px_rgba(var(--p),0.4)] hover:shadow-[0_0_30px_rgba(var(--p),0.6)] border-none text-white relative overflow-hidden group">
                        <span class="relative z-10">Lancer le protocole</span>
                        <div class="absolute inset-0 h-full w-full scale-0 rounded-2xl transition-all duration-300 group-hover:scale-100 group-hover:bg-white/20"></div>
                    </a>
                    
                    <a href="#services" class="btn btn-outline btn-lg hover:bg-white hover:text-black group">
                        <span class="group-hover:translate-x-1 transition-transform">Explorer les données →</span>
                    </a>
                </div>

                {# Décoration de code #}
                <div class="mt-12 opacity-30 text-xs font-mono select-none hidden md:block">
                    <p>initiating_sequence... [OK]</p>
                    <p>loading_assets... 100%</p>
                    <p>connecting_to_server... <span class="text-success">ESTABLISHED</span></p>
                </div>
            </div>
        </div>
    </div>

    {# 2. SERVICES SECTION (Dynamique avec vos données !) #}
    <section id="services" class="py-20 bg-base-100 px-4">
        <div class="max-w-7xl mx-auto">
            <div class="text-center mb-16">
                <h2 class="text-3xl font-bold mb-4">Nos Expertises</h2>
                <p class="text-gray-500">Tout ce dont vous avez besoin pour réussir en ligne.</p>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                
                {# On boucle sur les prestations de la base de données #}
                {# templates/home/index.html.twig #}

{% for prestation in prestations %}
    <div class="card bg-base-200 shadow-xl hover:shadow-2xl hover:-translate-y-1 transition-all duration-300 border border-base-300">
        <div class="card-body">
            
            {# --- ZONE ICONE (Dynamique selon le titre) --- #}
            <div class="mb-4">
                {% if 'Mobile' in prestation.titre or 'App' in prestation.titre %}
                    {# Icône MOBILE (Smartphone) #}
                    <div class="p-3 bg-primary/10 rounded-xl w-fit">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8 text-primary">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 1.5h3a2.25 2.25 0 0 1 2.25 2.25v13.5a2.25 2.25 0 0 1-2.25 2.25h-3a2.25 2.25 0 0 1-2.25-2.25V3.75a2.25 2.25 0 0 1 2.25-2.25ZM12 18v.01" />
                        </svg>
                    </div>
                {% else %}
                    {# Icône PAR DÉFAUT (Ordinateur / Web) #}
                    <div class="p-3 bg-secondary/10 rounded-xl w-fit">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8 text-secondary">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M9 17.25v1.007a3 3 0 0 1-.879 2.122L7.5 21h9l-.621-.621A3 3 0 0 1 15 18.257V17.25m-9-12V15a2.25 2.25 0 0 0 2.25 2.25h9A2.25 2.25 0 0 0 19.5 15V5.25m-9-12h9A2.25 2.25 0 0 1 21 7.5v7.5a2.25 2.25 0 0 1-2.25 2.25h-9a2.25 2.25 0 0 1-2.25-2.25v-7.5a2.25 2.25 0 0 1 2.25-2.25Z" />
                        </svg>
                    </div>
                {% endif %}
            </div>

            <h2 class="card-title text-base-content mb-2">{{ prestation.titre }}</h2>
            <p class="mb-6 text-base-content/70">{{ prestation.descriptionCourte }}</p>
            
            <div class="card-actions justify-end mt-auto">
                <a href="{{ path('app_prestation_show', {slug: prestation.slug}) }}" class="btn btn-sm btn-outline btn-primary group">
                    En savoir plus 
                    <span class="group-hover:translate-x-1 transition-transform">→</span>
                </a>
            </div>
        </div>
    </div>
{% else %}
    {# S'il n'y a pas encore de services #}
    <div class="col-span-3 text-center py-10">
        <div class="text-5xl mb-4">👻</div>
        <p class="text-lg text-base-content/60">Aucun service disponible pour le moment.</p>
    </div>
{% endfor %}

            </div>
        </div>
    </section>

{% endblock %}