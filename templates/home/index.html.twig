{% extends 'base.html.twig' %}

{% block title %}Accueil - Crea'Tech{% endblock %}

{# On connecte le fichier CSS qu'on vient de remplir #}
{% block stylesheets %}
    {{ parent() }} 
    <link rel="stylesheet" href="{{ asset('css/home.css') }}">
{% endblock %}

{% block body %}

<div class="hero-section">
    <h1>Bienvenue chez Crea'Tech 🚀</h1>
    <p style="font-size: 1.2rem; margin-top: 10px;">Votre partenaire digital.</p>
</div>

<div class="container">
    <h2 style="text-align: center; margin-bottom: 40px; color: #333;">Nos Expertises</h2>

    <div class="grid-services">
        {% for prestation in prestations %}
            <div class="service-card">
                <h3 class="service-title">{{ prestation.titre }}</h3>
                <p class="service-desc">{{ prestation.descriptionCourte }}</p>
                <a href="#" class="btn-details">En savoir plus →</a>
            </div>
        {% else %}
            <p style="text-align: center;">Aucun service n'est disponible.</p>
        {% endfor %}
    </div>
</div>

{% endblock %}